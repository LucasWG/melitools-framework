A seguir está o **PROMPT FINAL COMPLETO** do seu projeto **Melitools Framework**, já consolidando **tudo que você definiu**:

* framework **open**
* **TypeScript**
* **build para Tampermonkey**
* **plugins remotos do GitHub**
* **SHA256 integridade**
* **cache**
* **hot reload**
* **tema Mercado Livre futurista**
* **primeiro plugin interceptador**

Também segue o padrão correto de **userscript metadata header**, que é o bloco `// ==UserScript==` usado por gerenciadores como Tampermonkey para definir nome, versão, permissões e páginas onde o script roda. ([violentmonkey.github.io][1])

---

# PROMPT FINAL — MELITOOLS FRAMEWORK

---

# Visão Geral

Melitools é um **framework modular de automação para Tampermonkey**, projetado para:

* automação de workflows
* carregamento dinâmico de plugins
* atualização automática
* execução isolada
* desenvolvimento rápido

O framework possui:

* **core compilado**
* **plugins remotos carregados dinamicamente**
* **hot reload**
* **sistema de integridade SHA256**

O framework é **livre e open**.

Não existe:

* sistema de licença
* DRM
* autenticação obrigatória

---

# Arquitetura Geral

```
Melitools Framework
│
├ Core Engine
├ Plugin Loader
├ Plugin Cache
├ Integrity System (SHA256)
├ Plugin API
├ Event Bus
├ Update Manager
├ Hot Reload System
├ UI System
└ Theme System
```

---

# Tecnologias

```
TypeScript
pnpm
esbuild
Tampermonkey
```

---

# Estrutura do Projeto

```
melitools-framework
│
├ src
│
│  ├ core
│  │
│  │   framework.ts
│  │   loader.ts
│  │   update-manager.ts
│  │   cache.ts
│  │   integrity.ts
│  │   sandbox.ts
│  │   plugin-api.ts
│  │   event-bus.ts
│  │   hot-reload.ts
│  │
│  ├ plugins
│  │
│  │   plugin-list.json
│  │   interceptor.js
│  │
│  ├ ui
│  │
│  │   theme.ts
│  │   dashboard.ts
│  │
│  └ index.ts
│
├ tampermonkey.user.js
├ build.config.ts
├ tsconfig.json
└ package.json
```

---

# Distribuição do Framework

A distribuição contém **apenas o core compilado**.

Plugins **não são incluídos no build**.

O framework baixa plugins diretamente do GitHub.

---

# Repositório de Plugins

Plugins ficam no mesmo repo:

```
https://github.com/LucasWG/melitools-framework
```

Localização:

```
src/plugins
```

---

# Registry de Plugins

Arquivo:

```
src/plugins/plugin-list.json
```

Exemplo:

```json
{
  "plugins":[
    {
      "name":"interceptor",
      "version":"1.0.0",
      "file":"interceptor.js",
      "sha256":"PLUGIN_HASH"
    }
  ]
}
```

---

# Download de Plugins

O framework consulta:

```
https://raw.githubusercontent.com/LucasWG/melitools-framework/main/src/plugins/plugin-list.json
```

Depois baixa o plugin:

```
https://raw.githubusercontent.com/LucasWG/melitools-framework/main/src/plugins/interceptor.js
```

---

# Fluxo de Inicialização

```
framework start
↓
load plugin registry
↓
compare cache
↓
download missing plugins
↓
verify SHA256
↓
store cache
↓
execute plugins
↓
framework ready
```

---

# Cache de Plugins

Plugins ficam armazenados usando API do Tampermonkey:

```
GM_setValue
GM_getValue
```

Exemplo:

```
plugin_cache = {
  interceptor:{
    version:"1.0.0",
    code:"...",
    sha256:"..."
  }
}
```

---

# Verificação de Integridade

Todos plugins possuem hash SHA256.

Antes de executar:

```
download plugin
↓
calculate sha256
↓
compare manifest
↓
execute
```

Implementação:

```ts
async function sha256(text:string){

 const data = new TextEncoder().encode(text)

 const hash = await crypto.subtle.digest("SHA-256",data)

 return [...new Uint8Array(hash)]
   .map(b => b.toString(16).padStart(2,"0"))
   .join("")
}
```

---

# Plugin Loader

Responsável por:

```
download
validate
cache
execute
```

Exemplo:

```ts
async function loadPlugin(plugin){

 const code = await fetch(plugin.url).then(r=>r.text())

 const hash = await sha256(code)

 if(hash !== plugin.sha256)
    throw new Error("Plugin integrity failed")

 executePlugin(code)

}
```

---

# Sandbox de Plugin

Plugins executam em contexto controlado.

```
sandbox
│
├ api
├ events
└ storage
```

Execução:

```ts
function executePlugin(code){

 const sandbox = {
   api: PluginAPI,
   events: EventBus
 }

 const fn = new Function("sandbox", code)

 fn(sandbox)

}
```

---

# Plugin API

Disponível para plugins:

```
api.log()
api.storage()
api.events()
api.ui()
```

---

# Event Bus

Sistema interno de eventos.

```
events.on()
events.emit()
events.off()
```

Exemplo:

```
events.emit("page-loaded")
```

---

# Sistema de Atualização

O framework verifica atualizações automaticamente.

Intervalo:

```
60s
```

Fluxo:

```
check plugin-list
↓
compare versions
↓
download plugin
↓
verify SHA256
↓
replace cache
↓
reload plugin
```

---

# Hot Reload

Quando um plugin muda:

```
unload plugin
↓
download new version
↓
reload plugin
```

Implementação:

```
setInterval(checkUpdates,60000)
```

---

# Sistema UI

Interface do framework:

```
Melitools Dashboard
│
├ Plugins
├ Updates
├ Logs
└ Settings
```

Hotkey:

```
CTRL + SHIFT + M
```

---

# Tema do Framework

Tema oficial:

```
Mercado Livre Futurista
```

Características:

```
glass UI
soft glow
rounded edges
neon accents
```

---

# Paleta de Cores

```
primary   #FFE600
secondary #2D3277
accent    #3483FA
bg        #0F1115
surface   #1A1D24
text      #EAEAEA
```

---

# Componentes UI

```
modal
toast
panel
floating menu
plugin dashboard
```

---

# Build System

Framework escrito em:

```
TypeScript
```

Build:

```
pnpm build
```

Pipeline:

```
typescript
↓
esbuild bundle
↓
merge header
↓
generate userscript
```

---

# Header Tampermonkey

Arquivo base:

```
tampermonkey.user.js
```

Exemplo:

```javascript
// ==UserScript==
// @name Melitools Framework
// @namespace https://github.com/LucasWG
// @version 1.0.0
// @description Plugin framework
// @match *://*/*
// @grant GM_setValue
// @grant GM_getValue
// @grant GM_xmlhttpRequest
// @grant GM_addStyle
// ==/UserScript==
```

Esse bloco de metadados é necessário para que o gerenciador de userscripts saiba **nome, permissões e páginas onde o script será executado**. ([MakeUseOf][2])

---

# Build Config

Exemplo:

```ts
import esbuild from "esbuild"
import fs from "fs"

await esbuild.build({
 entryPoints:["src/index.ts"],
 bundle:true,
 outfile:"dist/core.js",
 format:"iife"
})

const header = fs.readFileSync("tampermonkey.user.js","utf8")
const code = fs.readFileSync("dist/core.js","utf8")

fs.writeFileSync(
 "dist/melitools.user.js",
 header + "\n" + code
)
```

---

# Plugin Inicial

Plugin:

```
interceptor.js
```

Objetivo:

Interceptar entradas de:

```
CTRL + V
barcode scanner
```

---

# Sanitização

Regex:

```
/\D/g
```

Remove tudo que não for número.

---

# Padrões Detectados

ID pacote

```
11 dígitos
```

Chave rastreio

```
44 dígitos
```

---

# Comportamento

### ID

Redirecionar:

```
https://envios.adminml.com/logistics/package-management/package/{ID}
```

ou

```
https://shipping-bo.adminml.com/sauron/shipments/shipment/{ID}
```

---

### Chave

```
sanitizar
↓
preencher input ativo
↓
não redirecionar
```

---

# Código do Plugin

```javascript
export default function(api){

 function sanitize(value){
   return value.replace(/\D/g,"")
 }

 function detect(value){

   if(value.length === 11) return "package"

   if(value.length === 44) return "tracking"

   return null
 }

 function handle(value){

   const clean = sanitize(value)

   const type = detect(clean)

   if(!type) return

   if(type === "package"){

     const url =
     `https://envios.adminml.com/logistics/package-management/package/${clean}`

     window.location.href = url

     return
   }

   if(type === "tracking"){

     const el = document.activeElement

     if(el && (el.tagName==="INPUT"||el.tagName==="TEXTAREA")){

       el.value = clean

       el.dispatchEvent(new Event("input",{bubbles:true}))

     }

   }

 }

 document.addEventListener("paste",(e)=>{

   const text = e.clipboardData?.getData("text")

   if(text) handle(text)

 })

 let buffer=""
 let last=0

 document.addEventListener("keydown",(e)=>{

   const now = Date.now()

   if(now-last>50) buffer=""

   last=now

   if(e.key==="Enter"){
     handle(buffer)
     buffer=""
     return
   }

   buffer+=e.key

 })

}
```

---

# Resultado

O Melitools Framework terá:

```
core leve
plugins remotos
cache local
hot reload
integridade SHA256
UI moderna
automação logística
```

---

✅ Se quiser, posso também te entregar a **VERSÃO 2 DO PROMPT (muito mais poderosa)** com:

* **plugin permissions**
* **plugin dependencies**
* **plugin marketplace**
* **plugin dev mode (live reload instantâneo)**
* **CLI do framework**

que deixaria seu projeto **nível VSCode plugin architecture**.

[1]: https://violentmonkey.github.io/api/metadata-block/?utm_source=chatgpt.com "Metadata Block - Violentmonkey"
[2]: https://www.makeuseof.com/make-inspect-element-changes-permanent-tampermonkey/?utm_source=chatgpt.com "How to Make Inspect Element Changes Permanent in Your Browser with Tampermonkey"
